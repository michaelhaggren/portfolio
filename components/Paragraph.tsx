import styled from '@emotion/styled';
//* A component to justify the text so it jumps to next row instead of forcing scroll
const Paragraph = styled.p`
  text-align: justify;
`;

export default Paragraph;
